# Trellis Export (Fixture) — export/006-signature-affirmations-inline

WOS-T4 signature export fixture. `062-signature-affirmations.cbor` is a chain-derived catalog over a readable WOS `SignatureAffirmation` payload and `066-signed-acts.cbor` is the verifier-facing signing projection. `067-policy-closure.cbor` carries the effective admission-policy evidence used at export time, while verifier trust roots and adapter allowlists remain verifier-supplied configuration.
