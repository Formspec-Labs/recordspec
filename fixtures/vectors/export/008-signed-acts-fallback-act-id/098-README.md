# Trellis Export (Fixture) — export/008-signed-acts-fallback-act-id

WOS-T4 signature export fixture whose readable `SignatureAffirmation` payload has no shared signing act id. `066-signed-acts.cbor` uses `signed-act-projection-wos-formspec-v2` and derives `act_id` from sorted source references under `signed-act-projection-act-id-v1`.
